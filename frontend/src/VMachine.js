class VMachine {
  constructor(name, state, isFirewall, script) {
    this.name = name;
    this.state = state;
    this.isFirewall = isFirewall;
    this.script = script;

  }

}

export default VMachine;